apiVersion: v1
kind: Service
metadata:
  name: wallet-demo-keycloak
  labels:
    app: wallet-demo
    app.kubernetes.io/component: keycloak
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app: wallet-demo
    app.kubernetes.io/component: keycloak
